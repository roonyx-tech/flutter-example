name: e_shop_flutter
description: EShop.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  grouped_list: ^4.0.0
  get_it: ^7.2.0 
  lints: ^1.0.1
  equatable: ^2.0.3
  intl: ^0.17.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.0.2
  path: ^1.8.0
  shared_preferences: ^2.0.6
  google_fonts: ^2.1.0
  flutter_svg: ^0.22.0
  flutter_bloc: ^8.0.1
  cupertino_icons: ^1.0.2
  drift: ^1.2.0
  
dev_dependencies:
  build_runner: ^2.0.5
  drift_dev: ^1.2.0
  
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/ic/

  uses-material-design: true
  